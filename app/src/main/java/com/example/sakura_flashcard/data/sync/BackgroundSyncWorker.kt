package com.example.sakura_flashcard.data.sync

import android.content.Context

object BackgroundSyncWorker {
    fun schedulePeriodicSync(context: Context) {
        // No-op: sync disabled - using direct MongoDB backend
    }
}
